const { describe, it, expect, vi } = require("vitest");
const genieService = require("../genieService");
const sampleService = require("../sampleService");
const demoService = require("../demoService");

// This JS test file was replaced by an ESM .mjs test.
// Keeping this placeholder file to avoid duplicate-suite name collisions during
// test collection. The real tests are in 'genie.process.test.mjs'.
module.exports = {};
